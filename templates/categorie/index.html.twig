{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idcategorie</th>
                <th>Caregorie</th>
                <th>Tags</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.idcategorie }}</td>
                <td>{{ categorie.caregorie }}</td>
                <td>{{ categorie.tags }}</td>
                <td>{{ categorie.description }}</td>
                <td>
                    <a href="{{ path('categorie_show', {'idcategorie': categorie.idcategorie}) }}">show</a>
                    <a href="{{ path('categorie_edit', {'idcategorie': categorie.idcategorie}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}
{% block body %}
    <div class="container clearfix">

        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 text-center">
            {% for key, formation in formations %}


            {% endfor %}
        </div>

        <section id="inscription" class="bg-grey clr p0">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                {% for categorie in categories %}
                    <div class="col-lg-4 col-md-4 col-sm-4 col-xs-4 text-center" style="min-height: 380px;">
                        <div class="col-md-12 text-center">
                            <h4 class="text-capitalize"> > {{ categorie.caregorie }}  </h4>
                            <p class="text-justify"> {{ categorie.tags }}
                        </div>
                        <div class="col-md-12">
                            <a class="btn btn-primary col-md-4 col-md-offset-1" href="{{ path('categorie_show', {'idcategorie': categorie.idcategorie}) }}">show</a>
                            <a class="btn btn-primary col-md-4 col-md-offset-2 " href="{{ path('categorie_edit', {'idcategorie': categorie.idcategorie}) }}">edit</a>
                        </div>
                    </div>

                    {% else %}

                    <div class="">
                        <p>
                            no records found<
                        </p>
                    </div>


                {% endfor %}
                <a href="{{ path('categorie_new') }}">Create new</a>

            </div>



        </section>
    </div>
{% endblock %}
